<nav class="siteHeader-navegacion" role="navigation">
    <a href="{{ site.url }}" class="siteHeader-logoTipo">
        <img src="{{ site.url }}/assets/img/svg/logo-jr.svg" alt="{{ site.title }}">
    </a>
    <input type="checkbox" id="menu-toggle" class="siteHeader-menuToggle">
        <label for="menu-toggle" class="siteHeader-labelToggle"></label>
    </input>
    <ul class="siteHeader-menuPrincipal">
        {% for link in site.data.navegacion.site-header %}
            {% assign link_url = site.url | append: link.url %}
            <li class="siteHeader-menuPrincipal-listItem"><a href="{{ link_url }}" class="siteHeader-menuPrincipal-link{% if page.url == link.url %} activo{% endif %}">{{ link.title }}</a>
            {% if link.submenu %}
                <div class="siteHeader-subMenu">
                    <ul class="siteHeader-subMenu-list">
                {% for sublink in link.submenu %}
                    {% assign sublink_url = site.url | append: sublink.url %}
                    <li class="siteHeader-subMenu-listItem">
                        <a href="{{ sublink_url }}" class="siteHeader-subMenu-link{% if page.url == sublink.url %} activo{% endif %}{% if sublink.icon == null %} sin-icono{% endif %}">
                            {% if sublink.icon %}<svg><title>{{ sublink.title }}</title><use xlink:href="{% asset_path icons.svg %}#{{ sublink.icon }}"></use></svg>{% endif %}
                            {{ sublink.title }}
                        </a>
                    </li>
                {% endfor %}
                    </ul>
                </div>
            </li>
            {% else %}
            </li>
            {% endif %}
        {% endfor %}
    </ul>
</nav>