{%- assign sucursales = site.data.sucursales | sort: 'estado' -%}
{% capture jsonld %}"mainEntity": {
  "@type": "ItemList",
  "itemListElement": [
{% for sucursal in sucursales %}
    {
      "@type": "ListItem",
      "position": {{ forloop.index | plus: 1 }},
      "item": {
        "@type": "HomeGoodsStore",
        "@id": "{{ sucursal.slug | prepend: '/sucursales/' | prepend: site.url | append: '/#sucursal' }}",
        "name": "{{ site.title }} {{ sucursal.nombre }}"
      }
    },
{%- endfor -%}
  ]
}{% endcapture %}
{{ jsonld | strip_newlines | replace:',]',']' }}
