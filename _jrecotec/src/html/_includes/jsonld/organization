{
  "@context": "http://schema.org",
  "@type":"Organization",
  "@id": "{{ site.url }}/#organization",
  "url":"{{ site.url }}",
  "name": "{{ site.owner.name }}",
  "description": "{{ site.description }}",
  "email": "mailto:{{ site.owner.email }}",
  "logo": "{{ site.logo | prepend: site.url }}",
  "address": {
    "@type": "PostalAddress",
    "addressLocality": "{{ site.owner.direccion.ciudad }}",
    "addressRegion": "{{ site.owner.direccion.estado }}",
    "addressCountry": "MX",
    "postalCode": "{{ site.owner.direccion.cp }}",
    "streetAddress": "{{ site.owner.direccion.direccion1 }}, {{ site.owner.direccion.direccion2 }}"
  },
  "sameAs": [
    "{{ site.owner.facebook }}",
    "{{ site.owner.google.plus }}",
    "{{ site.owner.linkedin }}"
  ]
}