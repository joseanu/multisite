<div class="wrapper">
    <select name="selectSucursal" id="selectSucursal" class="siteHeader-topBar-selectSucursal">
    {% assign sucursales = site.data.sucursales | sort: 'ciudad' %}
    {% for sucursal in sucursales %}
        <option value="{{ sucursal.slug }}"{% if sucursal.slug == 'guadalajara' %} selected{% endif %}>Sucursal {{ sucursal.nombre }}</option>
    {% endfor %}
    </select>
    <span class="siteHeader-topBar-ubicacion"><a href="{{ site.url }}/sucursales/guadalajara.html"><svg class="icon"><use xlink:href="{% asset_path icons.svg %}#map-marker"></use></svg> Ubicación</a></span>
    <span class="siteHeader-topBar-telefono">{% include partes/telefono tel=site.owner.tel1 %}</span>
    <span class="siteHeader-topBar-correo">{% include partes/e-mail e-mail=site.owner.email %}</span>
</div>